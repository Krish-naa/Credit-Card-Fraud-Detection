services:
  - type: web
    name: credit-card-fraud-detection
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    # With Docker, the Python version is frozen inside the image (see Dockerfile),
    # so no PYTHON_VERSION / runtime.txt is needed. The host cannot override it.
    healthCheckPath: /health
